#!/bin/bash
########################################################################################################################

# Chemin d'appel du script :
script_dir="$(dirname $0)"

# Création des tags :
pushd "${script_dir}" 1> /dev/null && ctags -R --languages=python -f tags && popd 1> /dev/null

########################################################################################################################
#Fin du fichier.
########################################################################################################################
