window.addEventListener('load', function () {
    document.getElementById("age5").textContent = "quarante-deux ans (ici non plus, on ne peut pas utiliser get_age() vu qu'on est dans un script classique)";
})
